import React from 'react';
import AppHeader from './AppHeader';
import AppFooter from './AppFooter';

const App = (props) => (
  <>
    <AppHeader />
    <main>main</main>
    <AppFooter />
  </>
);

export default App;
