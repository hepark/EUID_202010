/* -------------------------------------------------------------------------- */

const initState = {}

const homeReducer = (state = initState, { type }) => {
  switch (type) {
    default:
      return state
  }
}

export default homeReducer
