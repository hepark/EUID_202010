import styled from 'styled-components/macro'

/* -------------------------------------------------------------------------- */

export default styled.span`
  overflow: hidden;
  position: absolute;
  clip: rect(0, 0, 0, 0);
  clip-path: circle(0);
  width: 1px;
  height: 1px;
  margin: -1px;
  margin: 0;
  padding: 0;
  border: 0;
  white-space: nowrap;
`
